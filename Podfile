# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-rn-spike' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-rn-spike
  pod 'React'

  target 'ios-rn-spikeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-rn-spikeUITests' do
    # Pods for testing
  end

end
